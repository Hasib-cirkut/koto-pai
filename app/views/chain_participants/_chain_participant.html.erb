<div id="<%= dom_id(chain, chain_participant) %>" class="flex items-center justify-between rounded-2xl border border-slate-800 bg-slate-950 px-4 py-2 text-sm">
  <span><%= chain_participant.user.email %></span>
  <span class="text-xs uppercase tracking-[0.2em] text-slate-500"><%= chain_participant.role %></span>
</div>
